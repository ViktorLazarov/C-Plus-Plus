#include <iostream>
#include <cmath>

using namespace std;

int main()
{

    double z;

    // z = max(5,7);
    // z = min(5,7);
    // z = sqrt(9);
    // z = pow(2, 4);
    // z = abs(-234);
    z = round();
    z = ceil();
    z = floor();
    

    cout << z;

    return 0;
}